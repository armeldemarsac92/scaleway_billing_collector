# Grafana Dashboards

This folder stores exported Grafana dashboard JSON files.

Current dashboard:

```text
scaleway-billing-collector.json
```

The dashboard uses Prometheus counters exposed by the collector and follows the
query patterns documented in [../../docs/grafana-promql.md](../../docs/grafana-promql.md).
